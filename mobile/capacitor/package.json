{
  "name": "deal-finder-capacitor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:web": "cd ../../client && npm install && npm run build && mkdir -p ../mobile/capacitor/build && cp -r ../../client/build/* ../mobile/capacitor/build/"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/cli": "^5.0.0"
  }
}