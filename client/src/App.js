import React, { useEffect, useState } from 'react';import axios from 'axios';const API = process.env.REACT_APP_API_BASE || 'http://localhost:10000/api';export default function App(){const [providers,setProviders]=useState([]);const [query,setQuery]=useState('');const [results,setResults]=useState(null);useEffect(()=>{axios.get(API + '/providers').then(r=>setProviders(r.data.providers)).catch(()=>{});},[]);async function doCompare(){const { data } = await axios.post(API + '/compare', { query, providers: [] });setResults(data);}return (<div style={{padding:20,fontFamily:'sans-serif'}}><h1 style={{color:'#6f3df0'}}>Deal Finder</h1><input value={query} onChange={e=>setQuery(e.target.value)} placeholder='what to search'/><button onClick={doCompare}>Compare</button><div>{providers.map(p=><div key={p.id}>{p.name}</div>)}</div>{results && <div><h3>Best: {results.best.site} - ₹{results.best.price}</h3></div>}</div>);}